# SQL-Window-Functions-for-Analytics
we will query the project-db database using SQL SELECT statement together with SQL window functions to get database insights. We will accomplish this by completing each task in the project:
1.Retrieve data from tables in the project-db database.

2.Use the ROW_NUMBER() function

3.Work with ranking functions

4.Use the NTILE() function to group result sets

5.Work extensively with aggregate window functions

6.Use aggregate functions with window frames

7.Work with grouping functions
